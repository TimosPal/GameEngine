# Perfomance
- Could change IWindow, Application, Logger to be polymorphic template to reduce overhead of virtual calls.
- Where i use IDS + maps i could use just arrays if i never use any other ID func (1,2,...,n) -> maybe not beneficial
- Cache class string name per class
- Switch cases for event handling if ids were constexpr
- In event queue, define unique event types (eg resize event spamming shouldnt be avoided)
- GO manager removal 3 vecs too slow
- GO manager forced to use heap allocations to return valid ptrs in createNewGO func
- Extend VBO to use other types on GPU (not constant per atrtibute for whole vertex)

# Refactoring
- Change from int to specific types (uint32 etc)
- vec2 for mouse cords
- IO utility abstract to 1 class if possible, no need to re-write clone/getID funcs like in IEvent / IComponent
- Add assertions [x]
- Abstract window and API types in APP. [x]
- Instead of using derived classes for renderer and window lib can utilize CRTP (eg Window<GLFWWindow> m_window), better interface abstraction.
- Not sure if i like the glviewPort update to be done through callbacks from the glfw window.
- Event handling needs a bit more work -> some events should not be asychronous (eg window resize should lead to an imediate redraw)
  Maybe i could set if an event is sychronous or not.
- Limited VBO buffers to float only, should be able to use other types. For now it's easier to avoid templates.

# Bugs
- currently there is no way to update window width and height when an update occures. Should make an instance function to retrieve the object and set data in callback
- Components in GOs update method is wrong! Shouldnt run each update per go like this. [x]
- go manager does not handle object creation within start and update methods properly [x]
- With current implementation, if user stores GO in global state, after internal moving the reference will be invalid ptr. How to store GOs without losing reference. [x]
- ID system does not retrieve lost ids. If i make many Resources each one wastes an id even if its temporary.

# Features
- combos in actions system.
- more callback support (key, mouse)