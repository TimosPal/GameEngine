# GameEngine

A lightweight 2D game engine written in C++ using OpenGL.

## Externals

The engine relies on a few minimal external libraries, all included via submodules:

- **GLFW** – window/context/input management  
- **GLAD** – OpenGL function loader  
- **stb_image** – image loading (used for textures)  
- **spdlog** – logging system